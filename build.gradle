buildscript {
    repositories {
        maven {
            url "https://plugins.gradle.org/m2/"
        }
    }
    dependencies {
        classpath "com.gradle.publish:plugin-publish-plugin:0.9.3"
    }
}

apply plugin: 'groovy'
apply plugin: "com.gradle.plugin-publish"

dependencies {
    compile gradleApi()
    compile localGroovy()
}

version = "0.3"
group = "com.gmaslowski.gradle.plugin.property"

pluginBundle {
    website = 'https://github.com/gmaslowski/property-gradle-plugin'
    vcsUrl = 'https://github.com/gmaslowski/property-gradle-plugin'
    description = 'Property Gradle Plugin'
    tags = ["properties"]


    plugins {
        propertyGradlePlugin {
            id = 'com.gmaslowski.gradle.plugin.property'
            displayName = 'Property Gradle Plugin'
        }
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.11'
}